var gulp = require('gulp');
    sass = require('gulp-sass');
gulp.task('example', function () {
    return gulp.src('sass', function () {
        return gulp.src('src/sass/style.sass')
            .pipe(sass())
            .pipe(gulp.dest('src/css'))

    })
});